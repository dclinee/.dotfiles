#!/usr/bin/env zsh# ======================# 别名配置# ======================# ----------------------# 1. 通用文件管理# ----------------------if (( $+commands[eza] )); then  alias ls='eza --group-directories-first --icons'  alias lt='eza --tree --level=2'  alias lU='eza -la --sort=none'  alias lM='eza -la --sort=modified'elif [[ "$(uname)" == "Darwin" ]]; then  # macOS BSD ls 不支持 --color=auto / --group-directories-first，用 -G 启用颜色  alias ls='ls -G'else  alias ls='ls --color=auto --group-directories-first'fialias ll='ls -l'             # 详细列表alias la='ls -a'             # 显示隐藏文件alias l='ls -la'             # 详细列表 + 隐藏文件alias l.='ls -la .*'         # 只显示隐藏文件alias lS='ls -laS'           # 按大小排序# ----------------------# 2. 导航别名# ----------------------alias ..='cd ..'alias ...='cd ../..'alias ....='cd ../../..'alias ~='cd ~'alias -- -='cd -'            # 快速返回上一个目录alias cd..='cd ..'           # 容错，处理输入错误alias dir='ls -la'# ----------------------# 3. 系统与工具别名# ----------------------alias grep='grep --color=auto'alias egrep='egrep --color=auto'alias fgrep='fgrep --color=auto'alias df='df -h'             # 显示磁盘使用情况alias du='du -h'             # 显示目录大小alias du1='du -h --max-depth=1'  # 只显示一级目录大小if command -v free > /dev/null 2>&1; then  alias free='free -h'         # 显示内存使用情况fialias psa='ps aux'           # 显示所有进程alias pst='ps aux | grep -i'  # 搜索进程if command -v htop > /dev/null 2>&1; then  alias top='htop'             # 使用 htop 替代 topfialias c='clear'alias cls='clear'alias q='exit'alias h='history'# ----------------------# 4. 安全操作# ----------------------alias rm='rm -i'             # 删除前确认alias cp='cp -i'             # 复制前确认alias mv='mv -i'             # 移动前确认alias mkdir='mkdir -p'       # 创建目录时自动创建父目录# ----------------------# 5. Git 别名# ----------------------alias g='git'alias gs='git status'alias ga='git add'alias gaa='git add .'        # 添加所有文件alias gc='git commit'alias gcm='git commit -m'    # 带消息的提交alias gcma='git commit -am'   # 添加并提交alias gp='git push'alias gpl='git pull'alias gco='git checkout'alias gcb='git checkout -b'  # 创建并切换分支alias gcd='git checkout develop'alias gcmain='git checkout main'alias gd='git diff'alias gds='git diff --stat'  # 只显示文件变更统计alias gl='git log --oneline --graph'  # 简洁的日志alias glo='git log --oneline'alias gll='git log --oneline --graph --all'  # 显示所有分支日志alias gbr='git branch'alias gba='git branch -a'     # 显示所有分支alias gbd='git branch -d'     # 删除分支alias gfa='git fetch --all'   # 拉取所有远程分支alias gsta='git stash'alias gstp='git stash pop'# ----------------------# 6. 开发相关# ----------------------# Python（仅在非虚拟环境中设置别名，避免绕过 venv 的 python/pip）if [[ -z "${VIRTUAL_ENV:-}" ]]; then  alias python='python3'  alias py='python3'  alias pip='pip3'fialias venv='python -m venv venv'alias venva='source venv/bin/activate'  # 激活虚拟环境# Node.jsalias npmr='npm run'alias n='npm'alias nr='npm run'alias y='yarn'alias yr='yarn run'alias pn='pnpm'alias pnr='pnpm run'# ----------------------# 7. Docker 相关# ----------------------alias d='docker'alias dc='docker-compose'alias dcb='docker-compose build'alias dcu='docker-compose up'alias dcd='docker-compose down'alias dcl='docker-compose logs'alias dps='docker ps'alias di='docker images'alias dk='docker kill'alias drm='docker rm'alias drmi='docker rmi'alias dnet='docker network'alias dv='docker volume'# ----------------------# 8. 网络相关# ----------------------alias ping='ping -c 5'        # 只发送5个包alias fastping='ping -c 100 -i 0.2'alias ip='ip -c'if command -v ip > /dev/null 2>&1; then  alias ifconfig='ip addr show'fiif command -v ss > /dev/null 2>&1; then  alias netstat='ss -tuln'      # 替代 netstatfi# alias ss='ss -tuln'  # 已移除: 避免覆盖系统 ss 命令alias wget='wget -c'         # 断点续传alias curlL='curl -L'        # 跟随重定向（不覆盖默认 curl，避免破坏 API 调用）# ----------------------# 9. 压缩与解压# ----------------------alias untar='tar -xvf'alias unzip='unzip'alias zip='zip -r'alias tgz='tar -czvf'alias tbz2='tar -cjvf'alias txz='tar -cJvf'# ----------------------# 10. 其他实用别名# ----------------------alias nano='nano -w'         # 禁用自动换行# 注: 已移除 alias which='which -a'，因为 zsh 中 which 是 builtin，alias 会失效或行为异常# 如需显示所有匹配，请用 whence -p 或 type -aalias path='printf "%s\n" ${PATH//:/ }'  # 分行显示 PATH（用 printf 替代 echo -e）kb() { echo $((1024 * $1)) }  # 快速计算字节大小if command -v md5sum > /dev/null 2>&1; then  alias md5='md5sum'fiif command -v sha256sum > /dev/null 2>&1; then  alias sha256='sha256sum'fi# 注: 已移除 alias sum='sum -s'，覆盖了系统 sum 命令# 如需 checksum，请用 md5/sha256 alias 或直接调用原命令alias b='bc -l'              # 启动计算器# ----------------------# 11. Zsh 特定别名# ----------------------alias zshrc='${EDITOR:-vi} ~/.zshrc'  # 快速编辑 zshrcalias zshenv='${EDITOR:-vi} ~/.zshenv'alias zlogin='${EDITOR:-vi} ~/.zlogin'alias zlogout='${EDITOR:-vi} ~/.zlogout'alias reload='source ~/.zshrc'     # 重新加载 zshrc# ----------------------# 12. Brew 相关# ----------------------alias br='brew'                # brew 简写alias ba='brew analytics'alias bac='brew autoremove'     # 自动移除无用依赖alias bcl='brew cleanup'        # 清理旧版本和缓存alias bcln='brew cleanup -n'    # 预览清理内容alias bd='brew doctor'          # 检查 brew 健康状态alias be='brew edit'            # 编辑 formulaalias bf='brew formula'         # 显示 formula 路径alias bi='brew install'         # 安装包alias binf='brew info'          # 显示包信息alias bl='brew list'            # 列出已安装的包alias blo='brew list --outdated' # 列出过时的包alias bmig='brew migrate'       # 迁移包到新位置alias bo='brew outdated'        # 检查过时的包alias bq='brew query'           # 搜索包alias brr='brew reinstall'       # 重新安装包alias bs='brew search'          # 搜索包alias bsf='brew search --formula' # 只搜索 formulaalias bsci='brew search --casks' # 只搜索 casksalias bsw='brew search --desc'  # 按描述搜索alias bsl='brew services list'  # 列出 brew 服务alias bss='brew services start' # 启动 brew 服务alias bsp='brew services stop'  # 停止 brew 服务alias bsr='brew services restart' # 重启 brew 服务alias bswt='brew switch'        # 切换包版本alias bu='brew update'          # 更新 brew 和包列表alias bup='brew upgrade'        # 升级所有过时的包alias buu='brew update && brew upgrade'  # 更新并升级所有包alias bupn='brew upgrade --dry-run' # 预览升级内容alias bupc='brew upgrade --cask' # 升级所有 casksalias bv='brew --version'       # 显示 brew 版本# Brew Bundle 相关alias bba='brew bundle'         # 运行 Brewfilealias bbc='brew bundle cleanup'  # 清理 Brewfile 中未列出的包alias bbd='brew bundle dump'     # 生成 Brewfilealias bbl='brew bundle list'    # 列出 Brewfile 中的包alias bbv='brew bundle --verbose' # 详细输出# Emacs 相关# Emacs Daemon 核心别名alias es="emacs --daemon"alias ekill="emacsclient -e '(kill-emacs)'"alias erestart="ekill && sleep 0.3 && es"# GUI 客户端（日常主力）alias e="emacsclient -c -n -a emacs"alias ec="$e"alias emg="$e"# 终端内客户端（git/ssh服务器）alias et="emacsclient -t -a emacs"alias emt="$et"# 独立进程（干净环境调试）alias en="emacs -nw"alias emnew="emacs"alias emmini="emacs -q -nw"# 配置快捷入口（适配你的dotfiles）alias eminit="et ~/.config/emacs/init.el"alias emearly="et ~/.config/emacs/early-init.el"alias emlisp="et ~/.config/emacs/lisp/"alias eminstall="et ~/.dotfiles/emacs/install.sh"alias emlog="less /tmp/dotfiles_emacs_install_*.log"# 默认编辑器export EDITOR="$et"export VISUAL="$e"# 智能替换原生emacs命令emacs() {  if [[ -n "$DISPLAY" || "$TERM_PROGRAM" =~ "iTerm|WezTerm|Terminal" ]]; then    emacsclient -c -n -a emacs "$@"  else    emacsclient -t -a emacs "$@"  fi}# ----------------------# 13. 平台特定别名在 platform/ 文件中定义# ----------------------